import { createTheme } from '@material-ui/core/styles';

// Crea un tema personalizzato per Material-UI
const theme = createTheme({
  palette: {
    primary: {
      main: '#0070f3',
    },
    secondary: {
      main: '#19857b',
    },
  },
});

export default theme;
